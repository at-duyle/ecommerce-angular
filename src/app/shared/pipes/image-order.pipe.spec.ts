import { ImageOrderPipe } from './image-order.pipe';

describe('ImageOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
