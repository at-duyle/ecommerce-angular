import { ReceiverDeliveryOrderPipe } from './receiver-delivery-order.pipe';

describe('ReceiverDeliveryOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ReceiverDeliveryOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
