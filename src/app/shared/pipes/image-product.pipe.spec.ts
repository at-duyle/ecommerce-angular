import { ImageProductPipe } from './image-product.pipe';

describe('ImageProductPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageProductPipe();
    expect(pipe).toBeTruthy();
  });
});
