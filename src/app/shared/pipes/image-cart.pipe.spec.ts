import { ImageCartPipe } from './image-cart.pipe';

describe('ImageCartPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageCartPipe();
    expect(pipe).toBeTruthy();
  });
});
