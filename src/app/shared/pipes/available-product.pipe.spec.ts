import { AvailableProductPipe } from './available-product.pipe';

describe('AvailableProductPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableProductPipe();
    expect(pipe).toBeTruthy();
  });
});
